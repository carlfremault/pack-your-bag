---
description: Guidance for the new User Data Service
globs: apps/user-data-service/**/*
alwaysApply: false
---

# User Data Guidelines

This service is currently being bootstrapped.

## Patterns to Mimic

- Use `apps/auth-service` and `apps/product-service` as the "Gold Standard" for structure.
- the `packages/nestjs-common` package holds shared NestJS components and should be used whenever possible to avoid code repetition
- Look at `apps/auth-service/src/common` and `apps/product-service/src/common` for Exception Filters, Interceptors, and Decorators that should be ported or shared.
- Unlike the other two services, the User Data Service uses a Mongoose connection to a MongoDB database
