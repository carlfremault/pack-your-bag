import { schemas } from '@repo/product-client';

export const createTripSchema = schemas.CreateTripDto;
export const updateTripSchema = schemas.UpdateTripDto;
export const updateTripItemStatusSchema = schemas.UpdateTripItemStatusDto;
