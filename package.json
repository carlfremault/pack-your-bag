{
  "name": "pack-your-bag",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "format": "prettier --write .",
    "check-types": "turbo run check-types",
    "test:setup": "cross-env NODE_ENV=test dotenv -e .env.test -o -- turbo run test:setup",
    "test": "cross-env NODE_ENV=test dotenv -e .env.test -o -- turbo run test",
    "test:watch": "cross-env NODE_ENV=test dotenv -e .env.test -o -- turbo run test:watch",
    "test:browser": "turbo run test:browser",
    "view-report": "turbo run view-report",
    "prepare": "husky",
    "pre-commit": "turbo run lint --filter=[{staged}] && lint-staged",
    "db:init": "node --experimental-strip-types scripts/database-setup.ts",
    "db:init:test": "cross-env NODE_ENV=test node --experimental-strip-types scripts/database-setup.ts",
    "db:migrate:auth": "turbo run migrate --filter=auth-service",
    "db:migrate:prod": "turbo run migrate --filter=product-service",
    "db:status:auth": "turbo run status --filter=auth-service",
    "db:generate:auth": "turbo run generate --filter=auth-service",
    "db:seed:auth": "turbo run seed --filter=auth-service"
  },
  "devDependencies": {
    "@types/node": "^25.0.3",
    "@types/pg": "^8.16.0",
    "cross-env": "^10.1.0",
    "dotenv": "^17.2.3",
    "dotenv-cli": "^11.0.0",
    "dotenv-expand": "^12.0.3",
    "husky": "^9.1.7",
    "lint-staged": "^16.2.7",
    "playwright": "^1.56.1",
    "prettier": "^3.6.2",
    "prisma": "^7.2.0",
    "ts-node": "^10.9.2",
    "turbo": "^2.6.2",
    "typescript": "^5.9.3"
  },
  "engines": {
    "node": ">=22.6.0"
  },
  "lint-staged": {
    "*.{js,ts,tsx}": [
      "prettier --write"
    ]
  },
  "packageManager": "yarn@4.11.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "dependencies": {
    "pg": "^8.16.3"
  }
}
